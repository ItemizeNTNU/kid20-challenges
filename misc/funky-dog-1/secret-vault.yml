provider:
  name: openfaas
  gateway: http://127.0.0.1:19999

functions:
  secret-vault:
    lang: python3
    handler: ./secret-vault
    image: secret-vault:latest
    secrets:
      - secret-password
      - secret-username
      - flag
